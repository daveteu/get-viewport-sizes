{
  "name": "get-viewport-sizes",
  "version": "1.0.0",
  "description": "Get correct viewport size and use it in CSS",
  "main": "dist/index.js",
  "scripts": {
    "prepublish": "babel src --out-dir dist",
    "build": "babel src --out-dist dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dave Teu",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-es2015": "^6.24.1"
  },
  "babel": {
    "presets": ["es2015"]
  }
}
